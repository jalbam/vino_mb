<?
    //Definimos la etiqueta (nombre) para el archivo indice (index.php):
    $label_index = "Index";

    //Definimos la etiqueta (nombre) para el archivo de productos (products.php):
    $label_products = "Products";

    //Definimos la etiqueta (nombre) para el archivo de cesta de compra (basket.php):
    $label_basket = "Basket";

    //Definimos la etiqueta (nombre) para el archivo de formalizar compra (checkout.php):
    $label_checkout = "Check Out";

    //Definimos la etiqueta (nombre) para buscar todos los productos (enlace a products.php?category=all):
    $label_products_all = "All";
    
    //Definimos el texto (value) para el boton de busqueda y para el verbo "buscar":
    $search_button_value = "Search";

    //Definimos el title para el boton de busqueda:
    $search_button_title = "Click here to start search";
    
    //Definimos el title para el boton de busqueda:
    $search_input_title = "Type here words to search";
    
    //Definimos la variable que indica donde uno se encuentra:
    $you_are_here = "You are here";

    //El title para la imagen de la cesta, para agregar un producto a la cesta:
    $add_to_basket_title = "Add to Basket";

    //Definimos el verbo a mostrar antes de la categoria o de elegir "ver" todos:
    $label_products_action = "See";

    //Definimos la etiqueta (nombre) para mostrar en el lugar del "nombre del producto" en la tabla:
    $table["name"] = "Name";

    //Definimos la etiqueta (nombre) para mostrar en el lugar del "descripcion del producto" en la tabla:
    $table["description"] = "Description";

    //Definimos la etiqueta (nombre) para mostrar en el lugar del "precio del producto" en la tabla:
    $table["price"] = "Price";

    //Definimos la etiqueta (nombre) para mostrar en el lugar del "categoria del producto" en la tabla:
    $table["category"] = "Category";

    //Definimos la etiqueta (nombre) para mostrar en el lugar del "comprar producto" en la tabla:
    $table["buy"] = "Buy";

    //Definimos la etiqueta (nombre) para mostrar en el lugar del "cantidad" en la tabla:
    $table["quantity"] = "Quantity";

    //Definimos la etiqueta (nombre) para mostrar en el lugar del "precio total" en la tabla:
    $table["total_price"] = "Sum";
    
    //Definimos el texto a mostrar cuando no hay resultados:
    $results_void_text = "Can't find any result";
    
    //Definimos el texto a mostrar cuando se realiza una busqueda invalida:
    $search_illegal = "Illegal search";

    //Definimos el texto a mostrar si la cesta esta vacia:
    $basket_void_text = "Basket is empty";

    //Definimos el numero de decimales a mostrar (para el precio):
    $decimal_numbers = 2;
    
    //Definimos el simbolo para separar decimales:
    $decimal_separator = ".";
    
    //Definimos el simbolo para separar miles:
    $miles_separator = "";

    //Texto a mostrar para "relizar compra":
    $to_buy_text = "Check out";
    
    //Texto a mostrar para el boton Editar en la cesta:
    $edit_button_value = "Edit";

    //Texto a mostrar para el boton Borrar en la cesta:
    $delete_button_value = "Delete";
    
    //Texto a mostrar antes del campo para rellenar del "Nombre", en el formulario de enviar pedido:
    $form_name_label = "Name";

    //Texto a mostrar antes del campo para rellenar de "Apellidos", en el formulario de enviar pedido:
    $form_last_name_label = "Last Name";

    //Texto a mostrar antes del campo para rellenar de la "Calle", en el formulario de enviar pedido:
    $form_street_label = "Street";

    //Texto a mostrar antes del campo para rellenar de la "Ciudad", en el formulario de enviar pedido:
    $form_city_label = "City";

    //Texto a mostrar antes del campo para rellenar del "Codigo Postal", en el formulario de enviar pedido:
    $form_zip_code_label = "Zip Code";

    //Texto a mostrar antes del campo para rellenar del "E-Mail", en el formulario de enviar pedido:
    $form_contact_email_label = "E-Mail";
    
    //Texto (value) a mostrar en el boton de enviar pedido:
    $send_button_label = "Send";

    //Error al mostrar cuando algo no se envia, antes del campo (ej.: "Campo" no enviado o vacio):
    $send_error = "is empty or invalid";

    //El error a mostrar si no puede enviarse el E-Mail de pedido:
    $send_email_error = "Can't Send E-Mail";

    //El texto a mostrar para "Reintentar" enviar el E-Mail en caso de error:
    $send_email_again = "Retry again";

    //El error a mostrar si se ha enviado bien el E-Mail de pedido:
    $send_email_ok = "Your data was sent correctly";

    //Palabra para "Errores":
    $send_errors_word = "Errors";
?>
